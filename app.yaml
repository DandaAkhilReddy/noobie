runtime: python39

env_variables:
  SECRET_KEY: "your-production-secret-key-here"
  DATABASE_URL: "sqlite:///gym_tracker.db"

automatic_scaling:
  min_instances: 1
  max_instances: 10

resources:
  cpu: 1
  memory_gb: 0.5
  disk_size_gb: 10